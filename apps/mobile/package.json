{
  "name": "@portal-do-psi/mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Casca nativa Android (Capacitor) do Portal do Psi — abre https://portaldopsi.com.br dentro de um WebView nativo, sem duplicar código do site.",
  "scripts": {
    "sync": "cap sync android",
    "open:android": "cap open android",
    "run:android": "cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/app": "^6.0.1",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.1.2"
  }
}
